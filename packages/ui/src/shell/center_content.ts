import { BaseElementEmpty } from "@websoil/engine";

export class CenterContent extends BaseElementEmpty {
    async render() {
        
    }
}

customElements.define('center-content', CenterContent);