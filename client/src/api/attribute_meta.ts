const AttributeMetaAPI = {
    get_all: async () => {
        
    },

    add: async (name: string, 
        description: string, 
        kind: 'text' | 'number' | 'date') => {
        
    },

    update: async (meta_id: number, updates: any) => {

    },

    remove: async (meta_id: number) => {

    }
}

export default AttributeMetaAPI;